# Used by BOTH docker-compose.pc.yml and docker-compose.server.yml — Docker
# Compose auto-loads a root .env regardless of which compose file you pass
# via -f. For local dev without Docker, see apps/server/.env.example and
# apps/web/.env.example instead.

# Server mode only — the domain Caddy requests a TLS certificate for.
NYXEL_DOMAIN=nyxel.example.com

# Optional but recommended in server mode — email address used for ACME account
# registration / certificate management in Caddy.
ACME_EMAIL=ops@example.com

# Server mode only — Postgres password (also used inside DATABASE_URL).
POSTGRES_PASSWORD=change-me

# Required in both modes — used to sign Better-Auth session tokens. Both
# compose files refuse to start without a real value here (docker-compose.pc.yml
# used to silently fall back to the well-known public value
# "dev-secret-change-me" — fixed, see docs/DEPLOYMENT_HARDENING.md). Generate
# a real one: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-to-a-long-random-string

# Required in both modes — encrypts secret-shaped DB columns at rest (model
# provider API keys, Obsidian REST key). Never reuse BETTER_AUTH_SECRET's
# value here — different secret, different rotation lifecycle. Generate a
# real one: openssl rand -base64 32
NYXEL_ENCRYPTION_KEY=change-me-to-a-different-long-random-string

# Optional — enables Anthropic cloud models in the model picker.
ANTHROPIC_API_KEY=
